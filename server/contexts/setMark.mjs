export const setMarkContext = async (readyDataMessage, msg, context) => {
    const { message } = msg;
  
    
    
    context = "default";
  
    return { readyDataMessage: r, context: context };
  };
  